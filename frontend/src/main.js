/**
 * Vue 3 application entry point.
 *
 * Mounts the root App component onto the #app div defined in index.html.
 */
import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')
